---
interface IProps {
  text?: string
}

const { text } = Astro.props as IProps
---

{
  typeof text !== 'undefined' && (
    <h1 class='mx-auto max-w-screen-xl px-4 lg:px-6 xl:px-0 text-5xl font-extrabold dark:text-white'>
      {text}
    </h1>
  )
}
